package gameOfLife.model;

/**
 * Pattern class containing default starting patterns,
 * i. e The Queen Bee Shuttle Pattern, Tumbler Pattern and Random Pattern where there's 50% for cell to be alive or dead
 */
public enum Pattern {
    RANDOM,
    QUEEN_BEE,
    TUMBLER
}
